[package]
name = "speckle-spec"
version = "0.1.0"
edition = "2024"

[dependencies]
speckle = { version = "0.1.0", path = "../speckle" }
speckle-macro = { version = "0.1.0", path = "../speckle-macro" }

[dev-dependencies]
insta = { version = "1.47.2", features = ["yaml"] }
